---
title: Hybrid Artificial Potential Fields and Spatio-Temporal Transformers for Real-Time AUV Path Planning
title_zh: 混合人工势场与时空变换器用于实时AUV路径规划
authors: "Khadija Rais, Abdelmadjid Benmachiche, Imene Soualmia"
date: 2026-07-27
pdf: "https://arxiv.org/pdf/2607.25056v1"
tags: ["query:ocean-ai"]
score: 9.0
evidence: AUV路径规划智能体海洋应用
tldr: 针对AUV在复杂水下环境中的实时路径规划问题，本文比较了13种算法并重点提出混合人工势场与时空Transformer的新框架。该方法融合反应式避障与全局路径优化，在五种高分辨率地形场景中实现了最短平均路径（943.15单位）、低碰撞率（0.031）和高效计算（0.96秒），显著优于经典图搜索、采样、元启发式及纯学习方法。该成果为资源受限的水下系统提供了兼顾实时性与全局最优的鲁棒解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.25056v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1501, \"height\": 986, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.25056v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1740, \"height\": 644, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.25056v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1940, \"height\": 479, \"label\": \"Table\"}]"
motivation: 现有路径规划方法在最优性、避障和计算效率间存在显著权衡，亟需一种能平衡实时性与全局性能的混合框架。
method: 提出混合人工势场与时空Transformer的框架，利用APF进行局部反应式避障，ST-Transformer学习全局时空依赖以优化路径。
result: 在五种水下地形场景中，混合方法平均路径943.15单位、碰撞率0.031、计算时间0.96秒，全部指标优于13种对比算法。
conclusion: 混合APF+ST-Transformer是实时AUV导航的有效主方案，能同时保障安全性、路径最优性与计算效率。
---

## 摘要
自主水下航行器（AUV）在复杂、非结构化环境中运行，高效且安全的路径规划对于任务成功和能量保存至关重要。本文对十三种路径规划算法进行了全面的比较评估，范围从经典图搜索方法（A*、Dijkstra）和基于采样的方法（RRT*）到元启发式算法（PSO、GA、ACO、BCO）以及基于学习的架构。特别强调了一种结合人工势场（APF）与时空（ST）变换器的混合方法。在五类导航场景的高分辨率水下地形图上进行评估，所有算法均实现了100%的任务完成率；然而，在路径最优性、碰撞避免和计算负载方面出现了显著的权衡。混合APF+ST变换器表现出卓越的平衡性能，实现了最短的平均路径长度（943.15单位）、较低的碰撞率（0.031）和高效的计算时间（0.96秒），优于需要后备机制的独立学习模型以及产生更高延迟的经典方法。尽管经典算法保证了无碰撞路径，但其过长的路径长度和处理时间使其不太适合动态水下作业。相反，元启发式方法引入了不适合严格能量约束的轨迹复杂性。基于这些发现，推荐混合APF+ST框架作为实时AUV导航的主要方法，为资源受限的水下系统提供了一种协调反应式障碍物规避与全局路径最优性的鲁棒解决方案。

## Abstract
Autonomous Underwater Vehicles (AUVs) operate in complex, unstructured environments where efficient and safe path planning is critical for mission success and energy conservation. This paper presents a comprehensive comparative evaluation of thirteen path planning algorithms, ranging from classical graph-search methods (A*, Dijkstra) and sampling-based approaches (RRT*) to metaheuristics (PSO, GA, ACO, BCO) and learning-based architectures. Special emphasis is placed on a proposed hybrid approach combining Artificial Potential Fields (APF) with a Spatio-Temporal (ST) Transformer. Evaluated across five navigation scenarios on high-resolution underwater terrain maps, all algorithms achieved 100\% task completion; however, significant trade-offs emerged in path optimality, collision avoidance, and computational load. The Hybrid APF + ST-Transformer demonstrated superior balanced performance, achieving the shortest average path length (943.15 units), a low collision rate (0.031), and efficient computation time (0.96 s), outperforming standalone learning models, which required fallback mechanisms and classical methods that incurred higher latency. While classical algorithms guaranteed collision-free paths, their excessive path lengths and processing times render them less suitable for dynamic underwater operations. Conversely, metaheuristic approaches introduced trajectory complexity unsuitable for strict energy constraints. Based on these findings, the Hybrid APF + ST framework is recommended as a principal approach for real-time AUV navigation, offering a robust solution that harmonizes reactive obstacle avoidance with global path optimality in resource-constrained underwater systems.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：自主水下航行器（AUV）在复杂、非结构化且无GPS的水下环境中执行任务，其路径规划必须同时满足安全（避障）、路径最优（节省能量）和实时性（计算开销低）等多重目标。现有方法往往在这些目标之间存在显著权衡：经典图搜索（A*、Dijkstra）虽保无碰撞，但路径长、计算慢；元启发式（PSO、GA等）轨迹复杂、能耗高；纯学习模型缺乏鲁棒性，需后备机制。
- **核心问题**：如何设计一种能够协同局部反应式避障与全局路径优化的混合框架，在真实高分辨率水下地形上实现实时、安全且节能的AUV导航。
- **整体含义**：本文通过系统比较13种算法，提出并验证了混合人工势场（APF）+时空Transformer（ST-Transformer）的有效性，为资源受限的水下系统提供了一种平衡性能的实用方案，并指出了不同场景下的推荐策略。

## 2. 论文提出的方法论

- **核心思想**：融合APF的快速反应式避障能力与ST-Transformer对长程时空依赖的建模能力，使安全性、路径平滑度与全局最优性共同优化。APF作为主控，ST-Transformer提供辅助引导信号。
- **关键技术细节**：
  - **环境表示**：二维网格高程图\( Z \in \mathbb{R}^{H \times W} \)，每个像素表示归一化高程（0-1），高值代表障碍/危险区。
  - **APF组件**：吸引力 \( F_{att} = g - p \)（指向目标），排斥力 \( F_{rep} = \nabla Z(x,y) \)（地形梯度）；组合力 \( F_{APF} = \lambda_{att}F_{att} - \lambda_{rep}F_{rep} \)。使用动量更新位置：\( v_{t+1} = \beta v_t + (1-\beta)\hat{F}_{APF} \)，\( p_{t+1} = p_t + \eta v_{t+1} \)。
  - **ST-Transformer**：以历史轨迹 \( [p_0,...,p_t] \) 和上下文向量 \( c=[s,g] \) 为输入，通过自注意力（\( \text{Attention}(Q,K,V) = \text{softmax}(QK^T/\sqrt{d})V \)）输出预测下一路点 \( \hat{p}_{t+1} \)，经tanh激活限制范围。
  - **混合控制律**：最终方向 \( d_{final} = w_1 F_{APF} + w_2 d_{learned} + w_3 d_{goal} \)，其中 \( w_1>w_2>w_3 \) 确保安全主导。
  - **自适应机制**：远距离时缩放吸引力；检测停滞时添加随机扰动以逃离局部极小值。
- **算法流程概述**：在每个时间步，APF计算力向量，ST-Transformer提供预测方向，两者加权合成后更新位置，直至接近目标（\( \|p_T - g\| < \epsilon \)）。

## 3. 实验设计

- **数据集与场景**：
  - 使用 992 × 992 的高分辨率水下地形网格图（未公开具体来源）。
  - 设计五种导航场景：Diagonal（对角线）、Cross（交叉）、Medium（中等）、Reverse（反向）、Long/Long Diagonal（长距离/长对角线）。
- **Benchmark**：对每种场景，所有算法均要求从指定起点到达终点，以100%任务完成率为基础。
- **对比算法（共13种）**：
  - 经典图搜索：A*, Dijkstra, Theta*
  - 采样方法：RRT*
  - 元启发式：PSO, GA, ACO, BCO
  - 学习/混合：ST-Transformer（未训练）, Hybrid APF+ST-Transformer, APF（独立）, APF+A*, Theta*+APF
- **评估指标**：平均路径长度（单位）、平滑度（越接近0越平滑）、碰撞率（路径上危险点比例）、计算时间（秒）。

## 4. 资源与算力

- 论文中**未明确说明**所使用的硬件型号（GPU/CPU）、数量、训练时长。
- 提及的ST-Transformer模型为“untrained”（未训练），因此推理开销极小（0.112 s平均）；混合框架计算时间0.96 s，表明其可在普通CPU上实时运行。
- 未提供任何关于模型训练的资源消耗信息（可能未涉及训练，仅推理）。

## 5. 实验数量与充分性

- **实验数量**：每类算法在5种场景下各运行一次（共13×5=65次实验），未提及重复次数（如多次运行取均值）。结果表（Table 1, Table 2）显示为平均性能。
- **消融实验**：包含对APF、ST-Transformer、APF+ST混合及其他混合变体（APF+A*, Theta*+APF）的比较，本质上涵盖了消融分析。
- **充分性判断**：
  - **优点**：对比算法类型齐全（经典、采样、元启发式、学习），场景覆盖简单到长距离；指标多维（长度、平滑、碰撞、时间）。
  - **不足**：
    - 缺少统计显著性检验（如置信区间、方差），无法判断性能差异是否可靠。
    - 实验仅在2D网格上，未引入3D动态、洋流、传感器噪声及车辆动力学。
    - 训练数据/模型细节缺失，未训练ST-Transformer的代表性存疑。
    - 未在真实AUV平台或高保真模拟器上验证。总体而言，实验设计较全面，但外部有效性受限。

## 6. 论文的主要结论与发现

- **最佳整体性能**：Hybrid APF+ST-Transformer以最短平均路径（943.15单位）、低碰撞率（0.031）和高效计算时间（0.96 s）在五项指标中取得最优平衡。
- **经典方法**：A*和Dijkstra零碰撞但路径长（>1100单位）且慢（>7秒），不适合动态环境；Theta*碰撞率高（0.378）。
- **元启发式**：PSO、GA等路径长度中等，但平滑度极差（>30），能耗高；BCO计算慢（11.4秒）。
- **纯学习（未训练ST-Transformer）**：速度极快（0.112 s）但需后备机制，碰撞率（0.044）可接受但并非最优。
- **推荐策略**：
  1. 实时导航：首选Hybrid APF+ST-Transformer或APF+A*。
  2. 快速探索/未知环境：RRT*（零碰撞，0.424 s）。
  3. 离线任务规划：A*/Dijkstra（保证最优安全）。

## 7. 优点

- **方法创新**：将传统APF的物理模型与Transformer的序列学习能力有机结合，实现了局部避障与全局优化的协同，且不依赖大量训练数据。
- **综合比较**：覆盖13种代表性算法，设置5种难度不同的场景，评估指标全面（路径长度、平滑度、碰撞率、时间），使不同范式的优劣清晰可见。
- **实际导向**：明确给出了基于性能的AUV导航策略选择指南，具有工程实用价值。
- **分析客观**：承认所有方法在任务完成率上等价，并公正指出各自局限，如APF的局部极小、经典方法的延迟等。

## 8. 不足与局限

- **仿真环境简化**：实验基于2D栅格地形，忽略了3D动态（如洋流、压力、声纳噪声）、车辆运动学/动力学约束（如转弯半径、最大速度）。结果可能无法直接迁移至真实海洋。
- **模型训练细节缺失**：ST-Transformer为“未训练”状态，其性能未代表学习方法的全部潜力；也未提供训练/验证集划分，削弱了与有训练模型的公平比较。
- **缺乏统计验证**：未报告多次运行的标准差或置信区间，难以判断算法性能差异是否显著。
- **计算资源未说明**：未明确运行平台，时间指标的可重复性待质疑。
- **场景数量有限**：仅5种固定地形场景，未验证对动态障碍、不同水下环境（沉船、珊瑚礁）的泛化能力。
- **实践验证缺失**：未在真实AUV或高保真模拟器（如Gazebo/Unreal）上测试，限制了结论的可靠性。

（完）
